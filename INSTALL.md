# AI-OCR 名片辨識系統部屬手冊 (INSTALL.MD)

本手冊詳述如何將此 GAS 專案與 AppSheet 整合，並配置 Gemini API。

## 第一步：準備 Google Apps Script (GAS) 環境

1. **建立 GAS 專案**：
   - 進入 [Google Apps Script 儀表板](https://script.google.com/)。
   - 點擊「新專案」。
2. **導入代碼**：
   - 將本儲存庫中的 `code.js` 內容複製到 GAS 編輯器的 `代碼.gs` 中。
   - 點擊左側「專案設定」(齒輪圖示)。
   - 勾選「在編輯器中顯示 appsscript.json 資訊清單檔案」。
   - 回到編輯器，將 `appsscript.json` 的內容覆蓋進去。

## 第二步：配置指令碼屬性 (Script Properties)

在「專案設定」最下方，新增以下「指令碼屬性」：

| 屬性名稱 | 說明 | 取得方式 |
| :--- | :--- | :--- |
| `GEMINI_KEY` | Google AI API 金鑰 | 前往 [AI Studio](https://aistudio.google.com/app/apikey) 申請。 |
| `GOOGLE_SHEET_ID` | 目標試算表 ID | 試算表網址中 `/d/` 與 `/edit` 之間的字串。 |
| `IMAGE_FOLDER_ID` | 圖片存放資料夾 ID | Google Drive 存放圖片資料夾網址最後一段字串。 |

## 第三步：部署 GAS 為網路應用程式

1. 點擊右上角「部署」 > 「新增部署」。
2. 類型選擇「網路應用程式」。
3. **執行身分**：選擇「我在 Google 的帳戶」。
4. **誰可以存取**：選擇「只有我自己」(AppSheet 會以開發者身分呼叫)。
5. 點擊「部署」並授權存取 Drive 與 Sheets。

## 第四步：AppSheet 自動化 (Automation) 設定

1. 在 AppSheet 編輯器進入 **Automation** > **Bots**。
2. 建立新 Bot，Event 選擇名片表單的 `Adds only`。
3. 新增一個 Step：**Run a task**。
4. 設定如下：
   - **Task type**: `Call a Script`。
   - **Data source**: 選擇您的試算表。
   - **Apps Script project**: 選擇剛才建立的 GAS 專案。
   - **Function Name**: `processBusinessCard`。
5. **Function Parameters**:
   - `imageFileName`: `[你的圖片欄位名稱]`。
   - `rowNumber`: `[_RowNumber]`。

## 第五步：測試

1. 在 AppSheet 中上傳一張名片照片。
2. 等待約 5-10 秒。
3. 檢查 Google Sheet 是否自動填入姓名、電話等資訊。
4. 若失敗，可至 GAS 的「執行項」查看 Log 記錄。

---
*Generated by linuxfab - 2026-02-09*
